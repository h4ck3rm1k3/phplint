DEFINITION MODULE Statements

IMPORT Types
IMPORT ParseDocBlock

VAR

	include_path: ARRAY OF STRING
	(* list of the packages file names that must be reacheable through
	the include_path directive because the path provided with the
	require_once statement was not absolute nor relative *)

	modules_abs_path: ARRAY OF STRING
	(* absolute path dirs of modules prototypes *)

	packages_abs_path: ARRAY OF STRING
	(* absolute path dirs of packages *)


FUNCTION ParsePackage(abs_pathfile: STRING, module: BOOLEAN): Package
(*
	Parse the package, that is a source file.

	Parameters:
	abs_pathfile: absolute pathfile of the file.
	module: TRUE if it is a module, that is an extension of PHP (mysql, etc.)
		FALSE if it is a source generated by user.
	
	Return: descriptive data about the parsed package.
*)

END
